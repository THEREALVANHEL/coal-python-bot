# Discord Bot Configuration (REQUIRED)
DISCORD_TOKEN=your_discord_bot_token_here
DISCORD_CLIENT_ID=your_discord_client_id_here

# MongoDB Database Configuration (OPTIONAL - uses memory storage if not provided)
# For persistent data storage, provide a MongoDB connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/botdatabase?retryWrites=true&w=majority

# Gemini AI Configuration (OPTIONAL - AI features disabled if not provided)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Bot Configuration
BOT_PREFIX=!
DEBUG=False
ENVIRONMENT=production
PORT=10000

# Optional: Redis Configuration (for caching)
REDIS_URL=redis://localhost:6379

# Optional: Additional API Keys for extended features
WEATHER_API_KEY=your_weather_api_key_here
NEWS_API_KEY=your_news_api_key_here

# Render Deployment Notes:
# 1. Set DISCORD_TOKEN in Render environment variables (required)
# 2. Set MONGODB_URI for persistent data (optional, uses memory if not set)
# 3. Set GEMINI_API_KEY for AI features (optional)
# 4. All other variables are optional and have defaults